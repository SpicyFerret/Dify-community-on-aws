# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
tfplan
*.tfplan

# Arquivos .env reais (somente os *.example sao versionados)
.env
.env.*
!.env.*.example

# Policies IAM com valores concretos (account id, region) - nao versionar.
# O template versionado e' infra/iam/ci-deploy-policy.json (com placeholders).
*.local.json
